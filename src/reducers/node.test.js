import node from './node';
import initialState from '../store/initialState';

describe('Actions', () => {
  it('Create addChild action', () => {
    //
  });

  it('Create remove action', () => {
    //
  });
});

describe('Node reducer', () => {
  it('Remove node', () => {
    //
  });

  it('Remove another node', () => {
    //
  });

  it('Remove node with children', () => {
    //
  });

  it('Remove root node', () => {
    //
  });
});
